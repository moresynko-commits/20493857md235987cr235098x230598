services:
  - type: worker
    name: mdcrx-services
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: BOT_TOKEN
        sync: false
    autoDeploy: true

