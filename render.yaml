services:
  - type: web
    name: mdcrx-services
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: BOT_TOKEN
        sync: false  # Set manually in Render dashboard
    autoDeploy: true

